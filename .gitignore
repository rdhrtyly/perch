# Never commit installed packages or secrets
node_modules/
.env

# Runtime stuff Perch creates on the server (clones, built sites, etc.)
data/*
!data/.gitkeep

# Full backups made by scripts/backup.sh
backups-full/

*.log
